import { Routes } from '@angular/router';
import { DrawControlsComponent } from './draw-controls.component';

const routes: Routes = [
  {
    path: 'draw',
    component: DrawControlsComponent
  },
];

export const DrawControlsRoutes = routes;
